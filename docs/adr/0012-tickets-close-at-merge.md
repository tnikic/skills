# Tickets Close at Merge

PR-linked tickets remain open and assigned through implementation and human review, then close only when the PR merges through `Closes #N`. This makes tracker state reflect delivered work rather than an agent's claim that implementation is finished.
