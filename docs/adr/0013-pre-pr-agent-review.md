# Pre-PR Agent Review

`implement` must run the Standards and Spec axes of `code-review` against the feature branch before opening a PR; Coverage belongs to `improve-codebase-architecture`, not the normal implementation gate. This keeps review quality enforcement before handoff without turning agent analysis into a CI or merge requirement.
